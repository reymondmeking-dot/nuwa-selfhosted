# Changelog

## 1.1.1 - 2026-07-11

- Fixed `--docker-only` when Docker preference scoring is disabled.
- Expanded documented short tag aliases such as `--tag ai` to canonical catalog tags.
- Added positive limit validation, safer data-directory handling, and clearer git failures.
- Escaped Markdown table cells and added regression tests to CI.
- Restricted catalog-generated Markdown links to credential-free HTTP(S) URLs.
